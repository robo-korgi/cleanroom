# Local Development Environment Variables
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and safe for secrets

# Vercel Deployment Protection Bypass (only needed if protection is enabled)
# Get from: Vercel Dashboard → Settings → Deployment Protection → Protection Bypass for Automation
VERCEL_AUTOMATION_BYPASS_SECRET=your-bypass-secret-here

# Local Supabase credentials (optional - if different from production)
# NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key
